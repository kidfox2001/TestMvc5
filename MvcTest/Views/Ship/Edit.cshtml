@model Model.tblShip

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>tblShip</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ship_id)

        @Html.HiddenFor(model => model.ship_status)

        @Html.HiddenFor(model => model.ship_receive_status)

        @Html.HiddenFor(model => model.ship_doc)

        @Html.HiddenFor(model => model.ship_date)

        @Html.HiddenFor(model => model.request_location)

        @Html.HiddenFor(model => model.location_id)

        @Html.HiddenFor(model => model.ship_sum_item)

        @Html.HiddenFor(model => model.ship_sum_cost)

        @Html.HiddenFor(model => model.update_date)

        @Html.HiddenFor(model => model.update_by)

        <div class="form-group">
            @Html.LabelFor(model => model.note, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.note, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.note, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
